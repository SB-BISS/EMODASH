import pymongoimport datetimefrom pymongo import MongoClient""" For PGGM """client = MongoClient("mongodb://127.0.0.1:27017/VERAPreProcessor")""" For APG """# client = MongoClient("mongodb://VeraPreProcessorUser:VeraIsCool2018!1@127.0.0.1:2727/VERAPreProcessor?authMechanism=SCRAM-SHA-1")""" Global in FeatureExtractor.py """db = client.VERAPreProcessorfeatures = db.features""" In function extract_features<x> in FeatureExtractor just before return statement """featureDataItem = {    "callagentid": "Niels",    "callid": "12345",    "calldatetime": datetime.datetime.now(),    "createdAt": datetime.datetime.utcnow(),    "data": "features" #replace string with features object }features.insert(featureDataItem)